# 金山PDF 11.x 右键菜单消失修复补丁

> 修复 Windows 11 24H2 / 25H2 上「金山PDF」（11.8.0.8845 独立版，即"金山PDF专业版/教育版"）右键菜单消失的问题。
> 只需补一条机器级 COM 注册，**原版菜单（含拆分合并、格式转换等全部官方子菜单）即可完整恢复**。

## 问题现象

- Windows 11 24H2 / 25H2 上，安装金山PDF后右键任何 PDF / 图片文件，都没有「金山PDF」快捷菜单
- **重装金山PDF无效**（安装器永远把关键注册写在用户级）
- 同一台电脑的 Windows 10 分区、或未升级的 Win11 23H2 虚拟机上，菜单完全正常

## 根因

**Windows 11 24H2 起，资源管理器不再解析"用户级（HKCU）COM 类注册"**（安全加固方向），而金山PDF安装器恰好把右键扩展的 CLSID 只注册在用户级：

```
菜单处理器键（会被枚举到）：
  HKCU\Software\Classes\*\shellex\ContextMenuHandlers\kwpspdfshellext
      -> {8FE8AC65-EE7F-4C29-AF72-D2BACB633558}

CLSID 实际注册位置（新系统不再为外壳扩展解析这里）：
  HKCU\Software\Classes\CLSID\{8FE8AC65-...}\InprocServer32
      -> ...\office6\kwpspdfshellext64.dll
```

于是 Explorer 枚举到处理器键后，解析其 CLSID 时被无视 → 扩展从未被加载（进程模块列表中该 DLL 永远不出现）。

**修复 = 把同一个 CLSID 补注册一份到机器级 `HKLM\SOFTWARE\Classes\CLSID\`，原版菜单立即恢复。**

## 证据链（双机对照实验，2026-08）

| # | 实验 | 结果 | 排除/证实 |
|---|------|------|-----------|
| 1 | 在 25H2 宿主机直接 COM 调用老 DLL（提权+非提权） | 完美生成全部菜单项 | DLL 与新系统**完全兼容** |
| 2 | 查 explorer 进程模块列表 | 金山 DLL 从未被加载 | 枚举/加载环节被跳过 |
| 3 | 与正常虚拟机(23H2)注册表逐字节对齐 | 仍失效 | 排除注册表结构差异 |
| 4 | 卸载重装金山PDF | 仍失效 | 排除安装损坏 |
| 5 | 卸载 2026-08 累积更新 KB5121003（build 26200.9168→.8973） | 仍失效 | 排除当月补丁 |
| 6 | 完全卸载 MEGAsync（嫌疑：其私有 VC 运行库驻留 explorer） | 仍失效 | 排除运行库劫持 |
| 7 | 解析 DLL PE 导入表 | 仅依赖 kernel32（静态链接） | 排除依赖缺失 |
| 8 | 清除全部重复注册、仅留唯一一份（HKCU） | 仍失效 | 排除重复去重 |
| 9 | **同一注册迁移到 HKLM 机器级 + CLSID 补入 HKLM** | **立即恢复** ✅ | **根因锁定** |
| 10 | 对照：单位 Win11 同样中招；单位 Win10、家里 23H2 虚拟机正常 | — | 与分支代差吻合 |

> 排查中先后验证并排除的假说还包括：重复CLSID去重、CRT运行库劫持、豆包/管家类右键接管、
> 8月安全更新回归、Listary钩子干扰等，均无实证。

## 受影响范围

| 环境 | 是否受影响 |
|------|-----------|
| Windows 11 24H2 (26100) / 25H2 (26200) | ✅ 受影响 |
| Windows 11 23H2 (22631) 及更早 | ❌ 不受影响 |
| Windows 10 全版本 | ❌ 不受影响 |

## 使用方法

1. 下载本仓库，双击 **`安装补丁.cmd`**（自动请求管理员权限、自动定位金山PDF安装目录）
2. 提示重启资源管理器时选 **Y**
3. 右键任意 PDF / 图片，原版「金山PDF」菜单回归

## 卸载方法

双击 **`卸载补丁.cmd`**，仅移除补丁添加的机器级 CLSID，其余一概不动。

## 常见问题

**Q: 重装金山PDF后需要重新打补丁吗？**
A: 不需要。补丁写入的 HKLM CLSID 指向的是安装目录里的 DLL 文件路径，只要安装路径不变就一直有效。路径变了重跑一次安装补丁即可。

**Q: 卸载 8 月安全更新能解决吗？**
A: 不能。已实测回退 KB5121003（build 26200.9168 → .8973）后菜单依旧缺失，说明问题出在 24H2 分支更早的变更中。安全更新请正常安装。

**Q: 多选文件右键合并，只进一个文件？**
A: 这是 Win11 新版菜单对静态/传统扩展的通病（逐文件触发），与金山无关。多文件场景建议进「PDF合并」窗口后再添加文件。

**Q: 未来会自己好吗？**
A: 如果金山官方在新版本中改用机器级注册（或微软调整策略），原版菜单自然恢复，届时本补丁可直接卸载，两者不冲突。

## 文件说明

| 文件 | 用途 |
|------|------|
| `安装补丁.cmd` | 入口（自动提权），调用 ps1 完成修复 |
| `安装补丁.ps1` | 核心逻辑：定位安装目录 → 写入 HKLM CLSID |
| `卸载补丁.cmd` | 移除补丁添加的注册 |

## 免责声明

本补丁仅向 `HKLM\SOFTWARE\Classes\CLSID\{8FE8AC65-EE7F-4C29-AF72-D2BACB633558}` 写入一条指向金山PDF官方签名 DLL 的注册项，不修改任何系统文件。可用卸载脚本完整还原。

## 致谢

排查过程中使用的对照环境：Windows 11 23H2 虚拟机（Hyper-V）、Windows 10 物理机。
